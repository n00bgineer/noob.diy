// Don't forget to tell Prisma about your edits to this file using
// `yarn rw prisma migrate dev` or `yarn rw prisma db push`.
// `migrate` is like committing while `push` is for prototyping.
// Read more about both here:
// https://www.prisma.io/docs/orm/prisma-migrate

datasource db {
  provider = "sqlite"
  url      = env("DATABASE_URL")
}

generator client {
  provider      = "prisma-client-js"
  binaryTargets = "native"
}

model Post {
  id          String   @id @default(uuid()) // UNIQUE IDENTIFIER FOR EACH POST
  content     String   // CONTENT OF THE POST
  slug        String   @unique // URL-FRIENDLY IDENTIFIER
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
  published   Boolean  @default(false)
}

model DeletedPost {
  id          String   @id @default(uuid()) // UNIQUE IDENTIFIER FOR EACH DELETED POST
  content     String   // CONTENT OF THE POST
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
  slug        String   @unique
  deletedAt   DateTime @default(now()) // WHEN THE POST WAS DELETED (USED FOR 30-DAY EXPIRATION)
}
